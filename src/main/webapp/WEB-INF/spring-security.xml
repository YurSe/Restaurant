<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:bean="http://www.springframework.org/schema/security"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <bean:http auto-config="true" use-expressions="true" >
        <bean:intercept-url pattern="/pages/main.xhtml" access="permitAll" />
        <bean:intercept-url pattern="/pages/login.xhtml" access="permitAll"/>
        <bean:intercept-url pattern="/pages/register.xhtml" access="permitAll"/>
        <bean:intercept-url pattern="/admin**" access="hasAuthority('ADMIN')" />
        <bean:form-login login-page="/pages/login.xhtml"
                     default-target-url="/pages/main.xhtml"
                         login-processing-url="/j_spring_security_check"
                         authentication-failure-url="/pages/login.xhtml?status=error"
                         username-parameter="username"
                         password-parameter="password"
                          />
        <bean:logout logout-success-url="/pages/login.xhtml?status=logout"
                     delete-cookies="JSESSIONID"
                     />
        <bean:remember-me key="uniqueAndSecret"
                          token-validity-seconds="${jsessionId.valid.seconds}"/>
        <bean:csrf/>
    </bean:http>

    <bean:authentication-manager>
        <bean:authentication-provider user-service-ref="customUserDetailsService">
            <bean:password-encoder ref="encoder"/>
        </bean:authentication-provider>
    </bean:authentication-manager>

    <bean id="encoder"
          class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <context:component-scan base-package="by.restaurant.security"/>

</beans>